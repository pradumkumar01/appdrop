name: AppDrop
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  video_player:
  cached_network_image:
  carousel_slider:
  flutter_spinkit:
  flutter_advanced_drawer:
  permission_handler:
  dio:
  path_provider:
  flutter_launcher_icons: ^0.14.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 

# Correct place 
flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.png"

flutter:
  uses-material-design: true

  assets:
    - assets/json/jsonData.json
    - assets/images/
